spring:
  datasource:
    url: jdbc:mysql://localhost:3306/test_sql
    username: root
    password: 123
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    open-in-view: true
  redis:
  #  host: localhost
    host: 192.168.221.131
    port: 6379
    password:
    database: 0
    jedis:
      pool:
        max-active: 300
        max-idle: 100
        max-wait: 10000
        min-idle: 8
